# Copy 'sire' to somewhere in your BIN path, maybe in "/usr/local/bin/".
sudo cp sire /usr/local/bin/

# Create a folder called ".sire" in your home folder.
mkdir ~/.sire/

# Copy "sirerc" to your ".sire" directory.
cp sirerc ~/.sire/

# Create the empty file "siredb" in your ".sire" directory.
touch ~/.sire/siredb

# Edit the configuration file (sirerc) to fit your needs.
